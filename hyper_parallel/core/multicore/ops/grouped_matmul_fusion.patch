diff --git a/gmm/grouped_matmul/op_kernel/grouped_matmul.cpp b/gmm/grouped_matmul/op_kernel/grouped_matmul.cpp
index fbd7b6c..13692ca 100644
--- a/gmm/grouped_matmul/op_kernel/grouped_matmul.cpp
+++ b/gmm/grouped_matmul/op_kernel/grouped_matmul.cpp
@@ -15,7 +15,7 @@
 #include "grouped_matmul_utils.h"
 #include "grouped_matmul_antiquant.h"
 #include "grouped_matmul_vector.h"
-#include "grouped_matmul_tiling_key.h"
+// The enclosing HyperMegaMoe kernel supplies the combined tiling template.
 #include "grouped_matmul.h"

 #include "kernel_operator.h"
@@ -69,13 +69,13 @@ constexpr MatmulConfig A8W4_GMM_CFG_MDL_NEW = GetMmCFG();
 }

 template <bool trans = false>
-using xType = MatmulType<AscendC::TPosition::GM, CubeFormat::ND, DTYPE_X, trans>;
+using xType = MatmulType<AscendC::TPosition::GM, CubeFormat::ND, DTYPE_DISPATCH_TARGET, trans>;

 template <bool trans = false>
 using xTypeMSD = MatmulType<AscendC::TPosition::GM, CubeFormat::ND, DTYPE_WEIGHT, trans>;

 template <bool trans = false>
-using weightType = MatmulType<AscendC::TPosition::GM, wFormat, DTYPE_X, trans>;
+using weightType = MatmulType<AscendC::TPosition::GM, wFormat, DTYPE_WEIGHT, trans>;

 template <bool trans = false>
 using weightTypeMSD = MatmulType<AscendC::TPosition::GM, wFormat, DTYPE_WEIGHT, trans>;
@@ -84,7 +84,7 @@ using yType = MatmulType<AscendC::TPosition::GM, CubeFormat::ND, MM_DTYPE_Y>;

 using yTypeMSD = MatmulType<AscendC::TPosition::GM, CubeFormat::ND, int32_t>;

-using biasType = MatmulType<AscendC::TPosition::GM, CubeFormat::ND, DTYPE_BIAS>;
+using biasType = MatmulType<AscendC::TPosition::GM, CubeFormat::ND, float>;

 namespace {
     __aicore__ inline static constexpr MatmulApiStaticTiling GetGmmMatmulApiTiling(bool isND2NZ, bool transB) {
@@ -379,18 +379,32 @@ namespace {
         op.Process(workspace, &tPipe);                                                                 \
     } while (0)

-template <int D_T_A, int D_T_B, int D_T_Y, int TRANS_A, int TRANS_B, int GROUP_LIST_TYPE,
-          int IS_STATIC_TILING_API, int A8W4_KERNEL_TEMPLATE, int A16W8_KERNEL_TEMPLATE, int AIV_AIC_RATIO, bool IS_ENABLE_FIXED_AXIS>
-__global__ __aicore__ void grouped_matmul(GM_ADDR x, GM_ADDR weight, GM_ADDR bias, GM_ADDR scale,
+extern "C" inline __aicore__ void grouped_matmul(GM_ADDR x, GM_ADDR weight, GM_ADDR bias, GM_ADDR scale,
                                                      GM_ADDR offset, GM_ADDR antiquantScale, GM_ADDR antiquantOffset,
                                                      GM_ADDR groupList, GM_ADDR perTokenScale, GM_ADDR y,
-                                                     GM_ADDR workspace, GM_ADDR tiling)
+                                                     GM_ADDR workspace, GM_ADDR tiling, bool transA, bool transB,
+                                                     bool fp32Output = false)
 {
     TPipe tPipe;
     AscendCUtils::SetOverflow(1);
     KERNEL_TASK_TYPE_DEFAULT(KERNEL_TYPE_AIC_ONLY);
     GM_ADDR user1 = GetUserWorkspace(workspace);

+    if (transA) {
+        if (fp32Output) {
+            using yType = MatmulType<AscendC::TPosition::GM, CubeFormat::ND, float>;
+            GMM_CUBE_IMP(GMMProcess, true, false, false, matmulCFG);
+        } else {
+            GMM_CUBE_IMP(GMMProcess, true, false, false, matmulCFG);
+        }
+    } else if (transB) {
+        GMM_CUBE_IMP(GMMProcess, false, true, false, matmulCFGUnitFlag);
+    } else {
+        GMM_CUBE_IMP(GMMProcess, false, false, false, matmulCFGUnitFlag);
+    }
+
+#if 0  // Other upstream entry variants are not part of the HyperMegaMoe composition.
+
 #if (defined(__CCE_AICORE__) && __CCE_AICORE__ == 220) || (defined(__NPU_ARCH__) && (__NPU_ARCH__ == 3003 || __NPU_ARCH__ == 3113))
 #if defined(GMM_ANTI_QUANT_A8W4_MSD)
     // ANTIQUANT_A8W4
@@ -655,4 +669,5 @@ __global__ __aicore__ void grouped_matmul(GM_ADDR x, GM_ADDR weight, GM_ADDR bia
     }
 #endif
 #endif
-}
\ No newline at end of file
+#endif
+}
diff --git a/gmm/grouped_matmul/op_kernel/grouped_matmul.h b/gmm/grouped_matmul/op_kernel/grouped_matmul.h
index 59e0f9d..5e9a7a6 100644
--- a/gmm/grouped_matmul/op_kernel/grouped_matmul.h
+++ b/gmm/grouped_matmul/op_kernel/grouped_matmul.h
@@ -419,7 +419,7 @@ class GMMCompute {
     GlobalTensor<AT> xGm;
     GlobalTensor<BT> weightGm;
     GlobalTensor<BiasT> biasGm;
-    GlobalTensor<DTYPE_Y> yGm;
+    GlobalTensor<CT> yGm;
 #if defined(GMM_QUANT_INT8)
     GM_ADDR scaleTensorPtr;
     GlobalTensor<DTYPE_SCALE> scaleGm;
@@ -474,11 +474,6 @@ __aicore__ inline void GMMCompute<mmType, sync>::Init(GM_ADDR x, GM_ADDR weight,
     LocalTensor<uint8_t> buf = ubBuf.template Get<uint8_t>();
     mm.SetLocalWorkspace(buf);
 #endif
-#if defined(__CCE_AICORE__) && __CCE_AICORE__ == 220
-    if (gmmBaseParams->isOutputDisableL2Cache != 0) {
-        yGm.SetL2CacheHint(CacheMode::CACHE_MODE_DISABLE);
-    }
-#endif
 }

 template <typename mmType, bool sync>
@@ -491,16 +486,7 @@ __aicore__ inline void GMMCompute<mmType, sync>::SetGlobalBufferBias(uint32_t gr
         } else {
             biasGm.SetGlobalBuffer(GetTensorAddr<BiasT>(0, biasTensorPtr) + mnConfig.nAxisBaseOffset);
         }
-#if defined(__CCE_AICORE__) && __CCE_AICORE__ == 220
-        constexpr bool isBiasEpilogue_ =
-            AscendC::IsSameType<DTYPE_BIAS, bfloat16_t>::value &&
-            (AscendC::IsSameType<DTYPE_SCALE, bfloat16_t>::value || AscendC::IsSameType<DTYPE_SCALE, float>::value);
-        if constexpr (!isBiasEpilogue_) {
-            mm.SetBias(biasGm[tailN]);
-        }
-#else
         mm.SetBias(biasGm[tailN]);
-#endif
     }
 }

@@ -531,7 +517,7 @@ __aicore__ inline GlobalTensor<typename mmType::BT::T> GMMCompute<mmType, sync>:
     } else if (isA8W4FakeQuant) {
         weightGmLocal.SetGlobalBuffer(reinterpret_cast<__gm__ BT *>(weightTensorPtr) + mnConfig.wBaseOffset + wOffset);
     } else {
-        weightGmLocal.SetGlobalBuffer(GetTensorAddr<BT>(0, weightTensorPtr) + mnConfig.wBaseOffset + wOffset);
+        weightGmLocal.SetGlobalBuffer(reinterpret_cast<__gm__ BT *>(weightTensorPtr) + mnConfig.wBaseOffset + wOffset);
     }
     #if !(defined(ASCENDC_OOM) && ASCENDC_OOM == 1)
     if (mnConfig.blockDimM == 1) {
@@ -560,12 +546,12 @@ __aicore__ inline void GMMCompute<mmType, sync>::MMCompute(
     // init global buffer
     if (singleX == 0) {
         if (groupListType == GROUP_LIST_TYPE_SPARSE && groupType == 0) { // 0: split M
-            xGm.SetGlobalBuffer(GetTensorAddr<AT>(listIndex, xTensorPtr));
+            xGm.SetGlobalBuffer(reinterpret_cast<__gm__ AT *>(xTensorPtr) + listIndex);
         } else {
-            xGm.SetGlobalBuffer(GetTensorAddr<AT>(groupIdx, xTensorPtr));
+            xGm.SetGlobalBuffer(reinterpret_cast<__gm__ AT *>(xTensorPtr) + groupIdx);
         }
     } else {
-        xGm.SetGlobalBuffer(GetTensorAddr<AT>(0, xTensorPtr) + mnConfig.xBaseOffset);
+        xGm.SetGlobalBuffer(reinterpret_cast<__gm__ AT *>(xTensorPtr) + mnConfig.xBaseOffset);
     }
     GlobalTensor<BT> weightGmLocal = SetGlobalBufferW(groupIdx, tailN, mnConfig);
     mm.SetOrgShape(mnConfig.m, mnConfig.n, mnConfig.k);
@@ -583,12 +569,12 @@ __aicore__ inline void GMMCompute<mmType, sync>::MMCompute(
     SetGlobalBufferBias(groupIdx, tailN, mnConfig);
     if (singleY == 0) {
         if (groupListType == GROUP_LIST_TYPE_SPARSE && groupType == 0) {
-            yGm.SetGlobalBuffer(GetTensorAddr<CT>(listIndex, yTensorPtr));
+            yGm.SetGlobalBuffer(reinterpret_cast<__gm__ CT *>(yTensorPtr) + listIndex);
         } else {
-            yGm.SetGlobalBuffer(GetTensorAddr<CT>(groupIdx, yTensorPtr));
+            yGm.SetGlobalBuffer(reinterpret_cast<__gm__ CT *>(yTensorPtr) + groupIdx);
         }
     } else {
-        yGm.SetGlobalBuffer(GetTensorAddr<CT>(0, yTensorPtr) + mnConfig.yBaseOffset);
+        yGm.SetGlobalBuffer(reinterpret_cast<__gm__ CT *>(yTensorPtr) + mnConfig.yBaseOffset);
     }
     #if defined(GMM_ANTI_QUANT)
         mm.template IterateAll<false>(yGm[outOffset], 0, false, true);
